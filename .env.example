# Database Connection (Neon Postgres)
DATABASE_URL="postgres://user:password@host:port/database?sslmode=require"

# NextAuth / Security (Generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-super-secret-key-at-least-32-chars"
NEXTAUTH_URL="https://your-app-name.vercel.app"

# Initial Super Admin Credentials (Used for seeding)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="securepassword"

# Global WhatsApp API Defaults (Can be overridden per tenant)
# WHATSAPP_API_URL="https://api.example.com/send"
# WHATSAPP_API_KEY="xyz..."
# WHATSAPP_SENDER="919999999999"
